package crawler

import (
	"net/url"
	"testing"
)

func TestWorker(test *testing.T) {

}

/*
ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
        fmt.Fprintln(w, "Hello, client")
    }))*/
